docker network create easyadnet

docker run  \
    --network easyadnet \
    -p 8000:8000 \
    --name easyad \
    -v $(pwd):/app  \
    easyad

docker run \
    --network easyadnet \
    --name easyaddb \
    -e POSTGRES_USER=postgres \
    -e POSTGRES_PASSWORD=postgres \
    -e POSTGRES_DB=easyaddb \
    -v easyad_data:/var/lib/postgresql/data \
    -p 5432:5432 \
    -d postgres:16
